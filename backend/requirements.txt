# 后端依赖（国内镜像友好）
# 安装: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# Web 框架
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# 数据校验与配置
pydantic==2.9.2
pydantic-settings==2.6.1

# 数据库（异步驱动）
sqlalchemy[asyncio]==2.0.38
aiomysql>=0.2.1
# SQLite 异步驱动（无 Docker 环境用，pip install 时会自动安装）
aiosqlite>=0.20.0
# MySQL 8 默认 caching_sha2_password 认证依赖 cryptography
cryptography>=42.0.0

# HTTP 客户端（用于 DeepSeek）
httpx==0.27.0

# 工具
python-dotenv==1.0.1

# 测试（按需安装）
pytest==8.2.2
pytest-asyncio==0.23.7
